import React from 'react'
import ReactDOM from 'react-dom/client'
import BudgetTracker from './App.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <BudgetTracker />
  </React.StrictMode>,
)
